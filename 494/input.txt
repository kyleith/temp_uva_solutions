#include<stdio.h>
#include<string.h>
int main()
{
    int i,count=0,k;
    char a[100];
    gets(a);
    for (i=0; a[i]!='\0'; i++)
    if ((a[i]>=65 && a[i]<=90 || a[i]>=97 && a[i]<=122) && a[i+1]==' ')
    count++;
    count=count+1;
    printf("%d",count);

    return 0;
}
